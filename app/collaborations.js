////////////////////////////////////////////////////////////
// add a collaboration by calling `session` with parameters
// - date
// - duration of the collaboration session
// - array of collaborators
//
// EXAMPLE:
// session("2023-03-05", 1.5, [bertrand, iona]);
// session("2023-03-05", 1.0, [donald, greta, henry]);
////////////////////////////////////////////////////////////

session("2023-03-05", 1.5, [bertrand, iona]);
session("2023-03-05", 1.0, [donald, greta, henry]);
